# 재현자료 생성 프로그램 설명 및 개발 보고서

작성일: 2026년 7월 22일  
프로그램명: Synthetic Table Studio

## 1. 한눈에 보는 프로그램

Synthetic Table Studio는 개인정보가 들어 있는 표 형태의 원본 자료를 직접 배포하지 않고도, 원본과 비슷한 구조와 통계적 특징을 가진 **재현자료(합성데이터)**를 만드는 로컬 웹 프로그램입니다.

사용자는 CSV 또는 XLSX 파일을 올리고, 각 열의 의미와 지켜야 할 규칙을 확인한 다음, 원하는 행 수를 지정해 재현자료를 생성할 수 있습니다. 생성이 끝나면 품질 보고서를 확인하고 CSV 또는 Parquet 형식으로 내려받을 수 있습니다.

이 프로그램은 인터넷 서비스가 아니라 사용자 컴퓨터의 `localhost`에서 실행됩니다. 원본 파일 전체를 웹브라우저 메모리에 올리지 않고 작은 조각으로 나누어 처리하므로, 일반적인 브라우저 업로드 방식보다 대용량 자료에 적합합니다.

## 2. 왜 만들었는가

실제 조사자료나 행정자료에는 개인을 알아볼 수 있는 값, 드문 특성, 민감한 조합이 포함될 수 있습니다. 원본 자료를 그대로 공유하면 개인정보 침해 위험이 있지만, 자료를 전혀 공유하지 않으면 분석 방법을 검토하거나 교육·개발·업무 재현을 수행하기 어렵습니다.

재현자료는 이 사이의 간격을 줄이기 위한 자료입니다. 원본 행을 그대로 복사하는 대신 원본에서 관찰된 분포와 열 사이의 관계를 학습하여 새로운 행을 만듭니다. 이를 통해 다음과 같은 작업을 지원할 수 있습니다.

- 분석 코드와 업무 절차의 사전 점검
- 교육·실습용 표본 제공
- 시스템 개발과 화면 시험
- 원본 접근 전 분석 설계 검토
- 품질과 공개 가능성을 분리한 내부 검토

다만 재현자료라는 이름만으로 개인정보 보호가 자동으로 보장되는 것은 아닙니다. 이 프로그램은 그 차이를 명확히 보여주기 위해 **일반 고품질 합성**과 **형식적 차등프라이버시 합성**을 별도의 모드로 제공합니다.

## 3. 사용 흐름

프로그램은 복잡한 설정을 한 화면에 모으지 않고 여섯 단계로 나누어 안내합니다.

### 1단계. 원본 파일 업로드

CSV 또는 XLSX 파일을 선택합니다. 파일은 최대 64 MiB 크기의 조각으로 나뉘어 로컬 서버에 전송됩니다. 전송이 중단되면 이미 올라간 위치부터 이어서 보낼 수 있습니다.

CSV 파일은 문자 인코딩, 구분자, 첫 행의 열 이름 사용 여부를 확인합니다. XLSX 파일은 처리할 시트를 사용자가 직접 선택합니다.

### 2단계. 스키마 확인

프로그램은 각 열을 숫자, 날짜, 범주, 문자열, 식별자 등으로 제안합니다. 사용자는 제안을 그대로 믿지 않고 실제 의미에 맞게 수정해야 합니다.

예를 들어 숫자로 저장된 지역코드나 상품코드는 계산용 숫자가 아니라 범주 또는 코드일 수 있습니다. 주민번호나 내부 관리번호처럼 합성 모델이 학습해서는 안 되는 열은 식별자 또는 제외 열로 지정할 수 있습니다.

열이 많은 자료에서도 검토하기 쉽도록 열 이름 검색과 “확인이 필요한 열만 보기” 기능을 제공합니다.

### 3단계. 무결성 규칙 설정

재현자료가 업무상 반드시 지켜야 하는 조건을 규칙으로 지정합니다. 지원하는 규칙은 다음과 같습니다.

- 값이 비어 있으면 안 됨
- 허용된 값 목록만 사용
- 최솟값과 최댓값 범위 준수
- 여러 열의 조합 제한
- 특정 조건일 때 다른 열을 고정값으로 설정
- 여러 열의 합계가 목표 열과 일치
- 두 열의 대소 관계 유지
- 문자열 앞부분만 남기고 나머지 마스킹

서로 충돌하는 규칙, 같은 열을 동시에 바꾸려는 규칙, 순환 관계는 실행 전에 차단합니다. 생성 후에도 전체 결과를 다시 검사하며, 규칙을 만족하지 못한 후보 행은 최종 결과에서 제외합니다.

### 4단계. 생성 방식과 자원 설정

사용자는 일반 고품질 합성과 형식적 차등프라이버시 합성 중 하나를 선택합니다. 출력 행 수, 출력 형식, 학습 설정 등을 지정합니다.

형식적 차등프라이버시 모드에서는 공개 범주와 구간이 정의된 메타데이터 JSON이 추가로 필요합니다. 이 정보는 원본에서 몰래 추론하지 않고, 사용자가 공개 근거를 확인하고 등록해야 합니다.

### 5단계. 작업 진행 확인

준비, 학습, 생성, 규칙 보정, 평가, 내보내기, 게시 단계를 순서대로 보여줍니다. 브라우저를 새로 열어도 저장된 작업과 진행 상태를 복원할 수 있습니다.

작업을 취소한 경우 안전하게 재개할 수 있는 검증 지점이 있으면 새 작업 번호로 이어서 실행합니다. 완료·실패·취소 상태는 임의로 되돌리지 않습니다.

### 6단계. 보고서와 결과 다운로드

완료 후 생성 행 수, 규칙 위반 여부, 열별 분포 차이, 기준선 대비 차이 등을 확인할 수 있습니다. 결과 파일에는 SHA-256 체크섬을 기록해 내려받은 파일이 게시 당시 파일과 같은지 확인할 수 있습니다.

일반 합성 결과에는 “개인정보 보호 보장 없음” 경고가 표시됩니다. 형식적 차등프라이버시 결과에는 적용된 개인정보 보호 단위, 인접성, ε(엡실론), δ(델타), 공개 경계 통과 여부가 표시됩니다.

## 4. 기존 `reproduce_table.html`과 무엇이 다른가

새 프로그램은 기존 화면을 단순히 예쁘게 바꾼 것이 아니라, 생성 원리·개인정보 보호·품질 검증·대용량 처리 방식을 다시 설계한 프로그램입니다.

| 구분 | 기존 `reproduce_table.html` | Synthetic Table Studio |
|---|---|---|
| 실행 구조 | HTML 파일 하나에서 읽기·생성·평가·다운로드를 모두 수행 | 웹 화면, 로컬 API, 격리된 생성·평가 worker, 디스크 작업공간으로 역할 분리 |
| 파일 읽기 | CSV 파싱 결과와 XLSX workbook을 브라우저 메모리에 전부 적재 | 최대 64 MiB 청크로 재개 가능한 업로드 후 디스크에서 검사·Parquet 변환 |
| 생성 원리 | 원본 행 하나를 무작위로 복사한 뒤 숫자열마다 독립 Gaussian 잡음 추가 | 일반 모드는 TabularARGN으로 여러 열의 조건부 관계를 학습하고, DP 모드는 MST로 잡음이 들어간 저차원 분포를 학습 |
| 원본값 복제 위험 | 문자·범주값은 선택된 원본 행의 값을 그대로 사용하며 숫자도 원본값 주변에서 생성 | 모델 분포에서 새 행을 생성하고 식별자·희귀값·공개 경계를 별도로 관리 |
| 규칙 | 생성 뒤 마스킹·허용값·조건·합계를 순서대로 덮어씀 | 자료형이 있는 8개 규칙을 compile하고 충돌을 검사한 뒤 변환·전체 검증·잔여 위반 거절 수행 |
| 품질 평가 | 숫자 평균 차이를 “히스토그램 유사도”라고 표시하고, 문자열은 계산 없이 100%로 표시 | 실제 보류자료를 기준으로 KS·TVD·결측률·열 쌍·C2ST·실제 분석 성능을 분리 평가 |
| 상관관계 | 첫 번째와 두 번째 숫자열 한 쌍의 Pearson 상관만 비교 | 최대 2,415개 열 쌍의 Pearson·Spearman·결합분포·조건부분포와 전역 C2ST 평가 |
| 개인정보 지표 | 잡음 슬라이더 값으로 위험도를 선형 계산한 추정식 | 일반 모드는 Gower·Anonymeter 경험적 진단, DP 모드는 검증된 $(\epsilon,\delta)$ 회계와 release ledger 사용 |
| 작업 복구 | 페이지를 닫거나 새로고침하면 메모리 상태 소실 | 작업 상태·event·검증된 checkpoint를 기록하고 취소·재개 지원 |
| 결과 게시 | 브라우저 Blob으로 CSV와 TXT를 즉시 내려받음 | 임시 파일을 완성·검증한 뒤 CSV/Parquet과 SHA-256 manifest를 원자적으로 게시 |
| 대용량성 | 입력·합성 결과·다운로드 문자열을 모두 브라우저 메모리에 보관 | 브라우저 밖에서 batch·shard 단위 처리하고 RAM·디스크 admission을 먼저 검사 |

특히 기존 보고서의 “범주형 100% 보존”, “잡음값에 비례한 밀도·존재 노출 위험도”는 실제 공격 실험이나 수학적 개인정보 보호 계산이 아닙니다. 기존 숫자열 유사도도 평균값 차이만 사용하므로 분포의 모양, 꼬리, 여러 봉우리, 열 사이 관계가 달라도 높은 점수가 나올 수 있습니다. 새 프로그램은 이 값을 승계하지 않고, 계산 방법과 적용 가능 범위가 명시된 metric만 보고합니다.

## 5. 두 가지 생성 방식과 개인정보 보호 수준

### 일반 고품질 합성

일반 모드는 MOSTLY AI의 TabularARGN 엔진을 사용합니다. 원본의 개별 열 분포뿐 아니라 여러 열 사이의 조건부 관계를 자기회귀 방식으로 학습합니다. 쉽게 말해 앞에서 생성된 여러 값을 조건으로 다음 값을 생성하여 표 전체의 결합분포를 근사합니다.

장점은 복잡한 다변량 관계와 혼합 자료형의 분석적 특성을 보존하는 데 중점을 둘 수 있다는 것입니다. 그러나 현재 프로그램의 일반 모드는 DP 학습을 사용하지 않으므로 **수학적으로 계산된 개인정보 보호 보장은 없습니다**. 희귀값 억제, 학습 상한, 규칙 검사, 경험적 공격 진단은 위험을 줄이거나 찾는 장치이지 DP 보장을 대신하지 않습니다. 외부 공개 전에는 조직의 별도 심사가 필요합니다.

### 형식적 차등프라이버시 합성

차등프라이버시(DP)는 한 사람의 행이 원본에 추가되거나 빠져도 공개 결과의 확률분포가 크게 달라지지 않게 하는 수학적 기준입니다. 이 프로그램의 DP 모드는 `dpmm`의 MST 방식을 사용하며, 한 행을 한 보호 단위로 보는 `add/remove-one-row` 인접성을 적용합니다.

인접한 두 데이터셋 $D,D'$와 공개 결과의 집합 $S$에 대해 다음을 보장하는 것이 $(\epsilon,\delta)$-DP입니다.

$$
\Pr[M(D)\in S] \le e^\epsilon\Pr[M(D')\in S]+\delta
$$

- $\epsilon$이 작을수록 한 행의 영향 제한이 강하지만 일반적으로 잡음이 늘어 품질이 낮아질 수 있습니다.
- $\delta$는 위의 순수한 곱셈 경계에 더해지는 작은 완화항입니다. “재식별 확률”이나 “유출될 사람 비율”로 해석하면 안 됩니다.
- 화면 초기값은 $\epsilon=3$, $\delta=10^{-6}$입니다. 이때 $e^3\approx20.1$은 **결과 사건의 확률비 상한**이지 “20배 위험” 또는 “99.9999% 안전”이라는 뜻이 아닙니다.
- 적절한 값은 자료 민감도, 보호 대상 수, 공개 횟수, 허용 가능한 분석오차를 반영해 자료관리자가 사전에 승인해야 합니다.

MST는 공개하기로 확정된 범주와 숫자 구간을 사용해 1차·2차 주변분포를 선택하고, Gaussian 잡음을 더한 측정값으로 확률모형을 만든 뒤 행을 생성합니다. 검증된 실행에서는 총 $\rho$ 예산을 1차 측정·쌍 선택·2차 측정에 나누고 이를 요청한 $(\epsilon,\delta)$로 변환합니다.

DP 모드에서는 다음 경계를 지킵니다.

- 원본, 비공개 profile, private fit RNG가 든 checkpoint와 내부 진단은 공개 대상이 아닙니다.
- 범주와 숫자 구간은 사전에 공개되었다고 확인한 메타데이터만 사용합니다.
- 생성은 별도 fresh process에서 공개 sampling seed로 학습 난수 상태를 교체합니다.
- private source를 처음 읽기 직전에 예산을 사용한 것으로 기록합니다. 실패했더라도 이미 읽었다면 예산은 되돌리지 않습니다.
- 같은 privacy scope에서 여러 번 학습하면 현재 `basic_sequential` 회계가 사용된 $\epsilon$과 $\delta$를 각각 합산합니다.
- ledger가 `RELEASED`이고 `release_safe=true`, `contains_private_source_information=false`인 파일만 다운로드 목록에 표시합니다.

DP가 보장하는 것은 **한 행의 포함 여부가 공개 결과에 미치는 영향의 상한**입니다. 데이터 자체가 사실인지, 결과가 편향되지 않았는지, 공개 메타데이터가 안전한지, 특정 분석의 정확도가 충분한지는 별도 문제입니다.

## 6. 품질 평가는 어떤 metric을 사용하며 어떻게 읽는가

이 보고서에서 “재현성” 또는 “충실도”는 원본의 통계적 특성과 분석 결과를 얼마나 보존하는지를 뜻합니다. 분류모형 용어인 **recall(재현율)**과는 다릅니다. 현재 downstream 분류 평가는 accuracy를 사용하며 recall을 자동 계산하지 않습니다.

프로그램은 모든 용도를 대표한다는 단일 종합점수를 만들지 않습니다. NIST도 모든 분석에 유효한 보편적 utility metric은 없으며 실제 사용 목적에 맞춘 여러 metric을 함께 보아야 한다고 설명합니다.

### 6.1 먼저 실제 자료 자체의 흔들림을 기준선으로 삼음

원본을 `real_train_eval`과 한 번도 학습에 사용하지 않은 `real_holdout`으로 분리합니다. 학습자료와 보류자료 사이에도 표본오차가 있으므로, 합성자료의 차이를 0과만 비교하면 지나치게 엄격하거나 낙관적일 수 있습니다.

각 열에서 다음 값을 계산합니다.

$$
\text{baseline excess}=\max(0,\ d(\text{holdout},\text{synthetic})-d(\text{train},\text{holdout}))
$$

즉 합성자료가 자연스러운 train-holdout 차이보다 **얼마나 더 나쁜지**를 봅니다. 0에 가까울수록 좋습니다. 열 전체를 median, p95, maximum으로 요약하되 보편적인 합격점수로 합치지 않습니다.

### 6.2 기본 metric

| Metric | 무엇을 확인하는가 | 범위·해석 | 실제 사용 방법 |
|---|---|---|---|
| 행 수·schema·자료형 | 요청한 표 구조와 개수가 맞는지 | 정확히 일치해야 함 | 불일치하면 결과 게시를 실패 처리 |
| 규칙 위반 수 | 업무 규칙을 어긴 행이 있는지 | 0이어야 함 | 보정 후 전체 스캔에서 다시 검증 |
| 결측률 절대차 | 빈 값 비율 보존 | 0~1, 작을수록 좋음 | 열별 synthetic-holdout과 train-holdout 비교 |
| KS distance | 숫자·날짜·시간의 누적분포 차이 | 0~1, 0이면 표본 누적분포가 같음 | 평균뿐 아니라 위치·산포·분포 모양 차이 탐지 |
| TVD | 범주별 비율 차이의 총량 | 0~1, 0이면 비교한 범주 비율이 같음 | 상위 100개 범주와 `OTHER`를 비교하고 제외된 꼬리 질량도 표시 |
| Baseline excess | 자연 표본차를 넘는 합성 오차 | 0 이상, 작을수록 좋음 | 열별 값과 median·p95·maximum을 함께 확인 |

기본 평가는 각 population에서 최대 200,000행을 결정론적으로 표본 추출합니다. 같은 설정이면 같은 행이 선택되도록 sample hash와 seed를 기록합니다. 500회 percentile bootstrap으로 95% 신뢰구간을 계산합니다. 신뢰구간은 표본불확실성을 나타내며 모델 편향이나 개인정보 안전을 보장하지 않습니다.

### 6.3 고급 utility metric

고급 평가는 주 프로그램과 분리된 eval worker에서 제한된 표본으로 수행합니다.

| 평가 | 사용하는 metric | 읽는 법 |
|---|---|---|
| 숫자×숫자 열 쌍 | Pearson·Spearman 상관계수의 절대차 | 0에 가까울수록 선형·순위 관계 보존이 좋음 |
| 범주×범주 열 쌍 | 2차원 결합분포 TVD | 0에 가까울수록 범주 조합 비율이 비슷함 |
| 숫자×범주 열 쌍 | 20개 숫자 구간별 conditional TVD | 0에 가까울수록 숫자 수준에 따른 범주 구성 보존이 좋음 |
| C2ST | 실제/합성 판별기의 AUROC | 0.5에 가까우면 구별이 어렵고 1에 가까우면 쉽게 구별됨 |
| Downstream classification | TRTR·TSTR accuracy | 합성 학습(TSTR)이 실제 학습(TRTR)에 가까울수록 해당 분류 목적 보존이 좋음 |
| Downstream regression | TRTR·TSTR RMSE | 합성 학습의 RMSE가 실제 학습의 RMSE에 가까울수록 좋음; 둘 다 낮을수록 예측오차가 작음 |

- 열 쌍 평가는 최대 100,000행과 최대 2,415쌍을 사용합니다. 범주축은 최대 50개 값을 유지하고 나머지 질량을 별도로 기록합니다.
- C2ST는 선형 logistic regression과 비선형 histogram gradient boosting을 모두 사용합니다. 5개 seed, 층화 70/30 split, population당 최대 50,000행을 사용하며 AUROC에 500회 bootstrap 95% 신뢰구간을 붙입니다.
- C2ST에는 `real_train_eval vs untouched holdout` control도 함께 둡니다. 합성 AUROC만 0.5와 비교하지 말고 원본 두 표본도 실제로 얼마나 구별되는지 확인해야 합니다.
- Downstream 평가는 사용자가 목표 열과 classification/regression 과제를 직접 지정할 때만 실행합니다. 프로그램이 유리한 목표를 자동 선택하지 않습니다.
- TRTR은 실제자료로 학습해 실제 holdout에서 평가하고, TSTR은 합성자료로 학습해 **같은 실제 holdout**에서 평가합니다.

### 6.4 경험적 개인정보 metric

일반 합성의 내부 진단에만 다음 metric을 사용합니다.

| 진단 | 의미 | 주의 |
|---|---|---|
| Gower DCR | 각 합성행과 가장 가까운 실제 학습행 사이 거리 | 너무 작은 합성 DCR은 원본 근접복제 신호일 수 있음; 반드시 실제 holdout의 DCR과 비교 |
| Gower NNDR | 첫 번째 최근접거리 / 두 번째 최근접거리 | 0에 가까우면 특정 한 행에 유난히 가까울 수 있음; holdout control과 비교 |
| Anonymeter singling-out | 한 개인과 대응될 수 있는 특이 조합 공격 | main·baseline·control 및 excess risk와 95% Wilson 구간 확인 |
| Anonymeter linkability | 나뉜 보조정보를 같은 사람으로 연결하는 공격 | 사용자가 보조열 그룹을 명시한 경우에만 실행 |
| Anonymeter inference | 알려진 보조열로 비밀열을 추론하는 공격 | 비밀열·보조열을 사용자가 명시하며 자동으로 민감열을 고르지 않음 |

Gower 평가는 각 population 최대 5,000행을 사용하고 식별자·자유문자열은 제외합니다. Anonymeter는 최대 500회 공격을 3개 seed로 수행합니다. 이 결과는 알려진 공격 시나리오에 대한 **경험적 경고등**일 뿐 형식적 DP 보장이 아닙니다.

DP 공개 보고서는 원본과 holdout에서 새 통계를 뽑아 공개하지 않습니다. 현재 공개 가능한 DP 보고서는 생성행의 정확한 개수·schema·규칙 위반·content hash와 privacy ledger를 중심으로 구성됩니다. 원본 기반 utility metric 자체도 민감할 수 있기 때문에, 공개하려면 별도 DP 처리 또는 공개 승인 절차가 필요합니다.

### 6.5 실제 판정 순서

1. 행 수·schema·규칙 위반이 정확한지 확인합니다. 하나라도 실패하면 품질 비교 전에 결과를 폐기합니다.
2. 열별 KS/TVD·결측률과 baseline excess를 확인합니다. median만 보지 말고 p95·maximum으로 나쁜 열을 찾습니다.
3. 핵심 분석에 필요한 열 쌍과 C2ST를 확인합니다.
4. 실제 사용 목적이 분류·회귀라면 목표를 사전에 정하고 TRTR/TSTR을 비교합니다.
5. 일반 합성은 Gower·Anonymeter와 조직의 공개심사를 추가합니다.
6. DP 합성은 utility와 별개로 $\epsilon,\delta$, privacy unit, 누적 ledger, 공개 메타데이터를 확인합니다.

## 7. 개인정보를 얼마나 보장하면서 품질을 얼마나 보장하는가

결론부터 말하면 **하나의 백분율로 둘을 동시에 보장할 수 없습니다**.

| 생성 방식 | 개인정보에 대해 보장하는 것 | 품질에 대해 보장하는 것 |
|---|---|---|
| 일반 TabularARGN | 형식적 보장 없음. 경험적 진단과 공개심사만 제공 | 특정 데이터에서 관측한 KS/TVD·관계·C2ST·TRTR/TSTR와 신뢰구간을 보고 |
| DP MST | 설정한 row-level $(\epsilon,\delta)$-DP와 누적 회계를 보장 | schema·행 수·규칙은 정확히 검증하지만 통계적 충실도의 최저 백분율은 보장하지 않음 |

따라서 “프라이버시 95%, 재현율 90%” 같은 표현은 사용하지 않습니다. $\epsilon$은 보호 백분율이 아니고 KS·TVD·C2ST도 서로 다른 현상을 측정하기 때문입니다.

실무에서는 먼저 필요한 분석을 정하고 허용오차를 정한 뒤, 승인된 privacy budget 안에서 결과를 비교해야 합니다. 더 작은 $\epsilon$은 대체로 더 강한 보호와 더 큰 오차를, 더 큰 $\epsilon$은 대체로 더 나은 충실도와 더 약한 보호를 가져옵니다. 다만 실제 곡선은 행 수, 열 수, 범주 수, 희귀 조합, 분석 종류에 따라 달라집니다.

같은 비공개 자료로 여러 $\epsilon$을 시험하는 행위도 각각 private fit을 수행하면 예산을 소비합니다. 비교 실험 횟수와 총 예산을 먼저 정하고 ledger 합계 안에서 실행해야 합니다. 가능하면 공개·비민감 대리자료로 설정을 좁힌 뒤 비공개 자료의 실행 횟수를 최소화합니다.

## 8. 다른 합성 알고리즘과의 비교

아래 표는 이 데이터에서 직접 수행한 순위표가 아니라 **알고리즘 구조와 보장 범위의 비교**입니다. 동일한 train/holdout split, privacy budget, schema, 계산자원으로 head-to-head benchmark를 수행하지 않았으므로 특정 방법이 항상 더 좋다고 주장하지 않습니다.

| 방법 | 생성 원리 | 일반적 장점 | 한계·개인정보 보호 | 이 프로그램에서의 상태 |
|---|---|---|---|---|
| 기존 bootstrap+Gaussian | 원본 행 복원추출 후 숫자열 독립 잡음 | 매우 단순하고 빠름 | 범주값 직접 복사, 다변량 관계를 학습하지 않음, 형식적 DP 없음 | 대체됨 |
| Gaussian Copula | 각 열 분포를 변환하고 상관구조를 통계적으로 근사 | 빠르고 비교적 설명하기 쉬움 | 복잡한 다봉분포·비선형·희귀 조건부 관계 표현이 제한될 수 있으며 DP가 자동으로 생기지 않음 | 미사용 |
| CTGAN | 생성자와 판별자가 경쟁하며 혼합형 표 분포를 학습 | 불균형 범주와 다봉 연속형을 다루도록 설계 | 학습만으로 DP가 되지 않으며 별도 DP 학습·회계가 필요 | 미사용 |
| TabularARGN | 여러 열의 조건부 확률을 자기회귀 신경망으로 학습 | 혼합 자료형과 다변량 관계, 조건부 생성을 유연하게 처리 | 현재 구성은 형식적 DP가 아니며 공개 전 별도 심사 필요 | **일반 모드에 사용** |
| MST / Private-PGM | DP로 선택·측정한 저차원 주변분포에서 확률모형 구성 | 명시적 $(\epsilon,\delta)$ 회계, 표 자료의 저차원 관계 보존에 적합 | 높은 차수 관계·희귀 조합은 잡음과 제한된 tree 구조로 약해질 수 있음 | **형식적 DP 모드에 사용** |
| AIM | 분석 workload에 유용한 query를 반복적으로 선택·측정 | 정해진 통계 질의에 예산을 집중할 수 있음 | workload를 사전에 정해야 하며 구현·회계·release gate를 별도로 검증해야 함 | 설치된 경로의 동등 gate를 실행하지 않아 비활성 |

선택 기준은 명확합니다. 외부 공개에 수학적 보호가 필수면 검증된 MST 경로를 사용하고 품질 손실을 측정합니다. 내부 개발·분석에서 높은 충실도가 우선이면 TabularARGN을 사용하되, 결과를 안전하다고 간주하지 않고 공격 진단과 공개심사를 거칩니다. 특정 집계표가 가장 중요하다면 향후 AIM 같은 workload-aware 방법을 동일한 privacy budget에서 별도로 검증할 가치가 있지만, 현재 프로그램은 이를 지원한다고 표시하지 않습니다.

## 9. 대용량 자료를 위한 설계

이 프로그램은 약 3~4 GB, 최대 5,500만 행×70열을 목표 범위로 설계했습니다. 이를 위해 다음 원칙을 적용했습니다.

- 브라우저가 파일 전체를 메모리에 읽지 않고 조각 단위로 전송
- CSV와 XLSX를 디스크 기반으로 검사·변환
- 내부 표준 형식으로 Parquet 사용
- 학습 행 수에 상한을 두고 원본 전체와 분리된 표본만 모델에 전달
- 생성 결과를 작은 batch와 shard로 기록
- CSV와 ZIP64 결과도 임시 파일에서 완성·검증한 뒤 원자적으로 게시
- 디스크와 메모리 여유를 확인한 뒤 작업을 허용하는 resource admission 적용
- 취소·재시작 시 검증된 checkpoint만 재사용

Apple M4 Pro 환경에서는 승인된 약 99만 행 표본과 200만 행×70열 scale control을 검증했습니다. 하지만 이는 NVIDIA L40S 48 GB GPU 4장이 있는 production 환경의 처리 능력을 증명하는 결과가 아닙니다.

## 10. 보안과 개인정보 보호 경계

프로그램은 기본적으로 `127.0.0.1`에만 연결됩니다. 외부 네트워크 주소로 여는 기능은 명시적인 위험 옵션 없이는 거부됩니다.

주요 보호 장치는 다음과 같습니다.

- 실행할 때마다 새로 발급되는 localhost 전용 세션 쿠키
- 허용된 Host와 Origin 검사
- 작업 공간 밖의 경로 접근 차단
- 원자적 파일 게시와 체크섬 검증
- 다운로드 가능 여부와 외부 공개 가능 여부의 분리
- 내부 진단·로그·checkpoint의 다운로드 차단
- 원격 글꼴·스크립트·이미지를 사용하지 않는 보고서
- Content Security Policy 적용

이 프로그램의 보안 경계는 localhost 단일 사용자 환경을 전제로 합니다. 비-loopback 주소, 공동 서버, 네트워크 파일시스템에서 사용하려면 인증, TLS, 접근권한, 파일시스템의 원자성 등을 별도로 검증해야 합니다.

## 11. 개발 및 검증 결과

현재 코드에 대해 다음 검증을 수행했습니다.

- 백엔드 unit·integration·system 테스트: 207건 통과
- 고급 평가 contract 테스트: 7건 통과
- 실제 ARGN CSV 학습·생성·보고서 경로 확인
- 실제 DPMM MST fit·sample·ledger·release 경로 확인
- 개인정보 ledger와 공개 산출물 allowlist 검증
- Chromium, Firefox, WebKit에서 6단계 웹 흐름과 DP 보고서 복구 검증: 15건 통과
- Python 정적 검사와 format 검사 통과
- TypeScript typecheck와 ESLint 통과
- 웹 production build 통과

웹 차트 모듈은 보고서 화면을 열 때만 내려받도록 분리했습니다. 그 결과 기본 화면의 JavaScript bundle은 약 736.6 kB에서 약 250.3 kB로 줄었습니다.

## 12. 현재 확인된 한계

### Production GPU capacity는 아직 인증되지 않음

목표 최대 크기인 5,500만 행×70열의 production readiness를 주장하려면 NVIDIA L40S 48 GB×4 장비에서 실제 시험해야 합니다. 현재 실행 환경에는 해당 장비나 SSH 접근 정보가 없어 다음 항목은 아직 검증하지 못했습니다.

- 한 GPU에서 학습할 때 VRAM 40 GiB 미만 유지
- GPU 1·2·4개 생성 처리량 비교
- 5,500만 행 전체 생성·규칙 검사·내보내기·체크섬 시간
- production host의 RAM, NVMe, CPU, PCIe 구조에 따른 실제 상한
- 대규모 작업의 취소·재개 시간

따라서 M4 시험 결과만으로 “5,500만 행을 production에서 지원한다”고 표시해서는 안 됩니다.

### 합성데이터의 일반적 한계

- 일반 합성은 형식적 개인정보 보호를 보장하지 않습니다.
- DP 합성은 강한 보호 장치를 제공하지만 낮은 빈도의 세부 관계나 분석 정확도가 줄어들 수 있습니다.
- 잘못 지정한 열 의미나 규칙은 잘못된 재현자료로 이어집니다.
- 공개 메타데이터가 실제로 공개 가능한지에 대한 최종 책임은 자료 관리자에게 있습니다.
- 재현자료는 원본의 사실 확인, 개인 단위 의사결정, 공식 통계의 자동 대체물로 사용하면 안 됩니다.

## 13. 권장 운영 절차

1. 자료 관리자와 분석 담당자가 열의 의미와 공개 가능한 메타데이터를 함께 확인합니다.
2. 식별자와 제외 열을 먼저 지정합니다.
3. 원본에서 반드시 지켜야 할 업무 규칙을 등록합니다.
4. 적은 행 수와 짧은 학습으로 전체 흐름을 먼저 점검합니다.
5. 품질 보고서에서 기준선, 열별 차이, 규칙 위반을 확인합니다.
6. 일반 합성 결과는 별도의 개인정보·공개 심사를 진행합니다.
7. DP 결과는 ε, δ, 인접성, privacy unit과 누적 공개 예산을 기록합니다.
8. 공개할 때는 화면에 표시된 release boundary와 파일 체크섬을 다시 확인합니다.
9. 원본과 내부 작업 공간은 조직의 보존·폐기 정책에 따라 관리합니다.

## 14. 결론

Synthetic Table Studio는 대용량 표 자료를 대상으로 업로드, 구조 확인, 규칙 적용, 재현자료 생성, 품질 평가, 다운로드까지 하나의 흐름으로 제공하는 localhost 프로그램입니다.

가장 중요한 특징은 “재현자료이므로 안전하다”고 단정하지 않는다는 점입니다. 일반 고품질 합성과 형식적 차등프라이버시를 명확히 구분하고, 내부 자료와 외부 공개 가능 산출물 사이에 검증 가능한 경계를 둡니다. 또한 품질을 단일 점수로 축약하지 않고 분포, 열 관계, 분석 목적, 경험적 개인정보 진단을 나누어 보여줍니다.

현재 로컬 기능과 M4 검증 범위는 완성되었으며, 실제 최대 production 용량을 확정하기 위한 L40S 48 GB×4 장비 시험이 남아 있습니다. 이 제한을 지키는 조건에서 본 프로그램은 재현 가능한 분석 준비, 교육, 시스템 개발, 제한적 자료 공유를 지원하는 실용적인 도구로 사용할 수 있습니다.

## 15. 참고 자료

- NIST, [Utility Metrics for Differential Privacy: No One-Size-Fits-All](https://www.nist.gov/blogs/cybersecurity-insights/utility-metrics-differential-privacy-no-one-size-fits-all)
- Tiwald 외, [TabularARGN: A Flexible and Efficient Auto-Regressive Framework for Generating High-Fidelity Synthetic Data](https://arxiv.org/abs/2501.12012)
- McKenna·Miklau·Sheldon, [Winning the NIST Contest: A scalable and general approach to differentially private synthetic data](https://arxiv.org/abs/2108.04978)
- McKenna 외, [AIM: An Adaptive and Iterative Mechanism for Differentially Private Synthetic Data](https://arxiv.org/abs/2201.12677)
- SDV, [GaussianCopulaSynthesizer 공식 설명](https://docs.sdv.dev/sdv/modeling/single-table-synthesizers/gaussiancopulasynthesizer)
- Xu 외, [Modeling Tabular data using Conditional GAN](https://arxiv.org/abs/1907.00503)
- Anonymeter, [공식 저장소와 사용 설명](https://github.com/statice/anonymeter)
